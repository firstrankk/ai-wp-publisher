# ============================================
# AI WP Publisher - Production Environment
# ============================================
# คัดลอกไฟล์นี้เป็น .env.production แล้วแก้ไขค่าตามต้องการ
# cp .env.production.example .env.production

# ---- PostgreSQL ----
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_DB=wp_publisher
POSTGRES_PORT=5432

# ---- Redis ----
REDIS_PORT=6379

# ---- API (Express) ----
API_PORT=4000
# JWT secret สำหรับ authentication (ใช้ openssl rand -hex 32)
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32
# Encryption key สำหรับเข้ารหัส API keys (ต้องเป็น 64 hex characters, ใช้ openssl rand -hex 32)
ENCRYPTION_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32
# CORS origin (ใส่ domain ของเว็บ)
CORS_ORIGIN=https://your-domain.com

# ---- Web (Next.js) ----
WEB_PORT=3000
# URL สำหรับ NextAuth
NEXTAUTH_URL=https://your-domain.com
# NextAuth secret (ใช้ openssl rand -hex 32)
NEXTAUTH_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32
# URL ของ API (ไม่ต้องมี /api ต่อท้าย เพราะ axios จะเพิ่มให้อัตโนมัติ)
NEXT_PUBLIC_API_URL=https://your-domain.com

# ---- Nginx ----
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
